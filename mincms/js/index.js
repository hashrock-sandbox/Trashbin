var canvasfunc = function(){
/*
	ctx.strokeStyle = '#ddd';
	//ctx.clearRect(0,0,canv_w,canv_h);
	nz = 2;
	ctx.beginPath();
	for(var j=0; j<20; j++){
		ctx.lineWidth   = 1;
		ctx.moveTo(nx, ny);
		nx += Rnd(9)-4;
		ny += Rnd(9)-4;
		if(nx < 0){nx = 0};
		if(ny < 0){ny = 0};
		if(nx > canv_w){nx = canv_w};
		if(ny > canv_h){ny = canv_h};
		ctx.lineTo(nx,ny);
	}
	ctx.stroke();
	ctx.closePath();
*/
};


